name: Translator Push Build

on:
  push:
    branches:
      - 'main'

jobs:
  build-translator:
    name: Build Push (${{ matrix.os_name }}-${{ matrix.arch }})
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: windows-latest
            os_name: windows
            arch: x64
            py_arch: x64
          - runner: windows-latest
            os_name: windows
            arch: x86
            py_arch: x86
          - runner: macos-15-intel
            os_name: macos
            arch: x64
          - runner: macos-14
            os_name: macos
            arch: arm64
          - runner: ubuntu-latest
            os_name: linux
            arch: x64
          - runner: ubuntu-24.04-arm
            os_name: linux
            arch: arm64
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set build variables (commit-based version)
        shell: bash
        run: |
          SHORT_SHA="${GITHUB_SHA::7}"
          echo "GIT_VER=commit-${SHORT_SHA}" >> $GITHUB_ENV
          echo "OS_NAME=${{ matrix.os_name }}" >> $GITHUB_ENV
          echo "ARCH_NAME=${{ matrix.arch }}" >> $GITHUB_ENV

      - name: Set up Python (Windows)
        if: runner.os == 'Windows'
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          architecture: ${{ matrix.py_arch }}
          cache: 'pip'
          cache-dependency-path: src/requirements_translator.txt

      - name: Set up Python (macOS/Linux)
        if: runner.os != 'Windows'
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: src/requirements_translator.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r src/requirements_translator.txt pyinstaller

      - name: Install icon tools (macOS)
        if: runner.os == 'macOS'
        run: |
          python -m pip install pillow

      - name: Generate version info
        run: python src/gen_version_info.py
        env:
          TRANSLATOR_VERSION: ${{ env.GIT_VER }}

      - name: Generate macOS icon
        if: runner.os == 'macOS'
        run: python src/build_icon_icns.py

      - name: Build app (macOS)
        if: runner.os == 'macOS'
        working-directory: src
        run: |
          python -m PyInstaller --onefile app.py --name ethos-manual-translator --windowed --icon icon.icns

      - name: Build app (Windows)
        if: runner.os == 'Windows'
        working-directory: src
        run: |
          python -m PyInstaller --onefile --noupx app.py --name ethos-manual-translator --windowed --version-file version_info.txt --icon icon.ico --add-data "icon.ico;."

      - name: Build app (Linux)
        if: runner.os == 'Linux'
        working-directory: src
        run: |
          python -m PyInstaller --onefile app.py --name ethos-manual-translator

      - name: Package app (macOS)
        if: runner.os == 'macOS'
        shell: bash
        run: |
          ART_NAME="ethos-manual-translator-${GIT_VER}-${OS_NAME}-${ARCH_NAME}.zip"
          ART_PATH="dist/${ART_NAME}"
          mkdir -p dist
          if [ -d "src/dist/ethos-manual-translator.app" ]; then
            (cd src && zip -q -r "../${ART_PATH}" dist/ethos-manual-translator.app)
          else
            (cd src && zip -q -r "../${ART_PATH}" dist/ethos-manual-translator)
          fi
          echo "ART_NAME=${ART_NAME}" >> $GITHUB_ENV
          echo "ART_PATH=${ART_PATH}" >> $GITHUB_ENV

      - name: Package app (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $artName = "ethos-manual-translator-$env:GIT_VER-$env:OS_NAME-$env:ARCH_NAME.zip"
          $artPath = "dist\\$artName"
          New-Item -ItemType Directory -Path dist -Force | Out-Null
          Compress-Archive -Path "src\\dist\\ethos-manual-translator.exe" -DestinationPath $artPath -Force
          "ART_NAME=$artName" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          "ART_PATH=$artPath" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - name: Package app (Linux)
        if: runner.os == 'Linux'
        shell: bash
        run: |
          ART_NAME="ethos-manual-translator-${GIT_VER}-${OS_NAME}-${ARCH_NAME}.zip"
          ART_PATH="dist/${ART_NAME}"
          mkdir -p dist
          (cd src && zip -q -r "../${ART_PATH}" dist/ethos-manual-translator)
          echo "ART_NAME=${ART_NAME}" >> $GITHUB_ENV
          echo "ART_PATH=${ART_PATH}" >> $GITHUB_ENV

      - name: Upload app artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.ART_NAME }}
          path: ${{ env.ART_PATH }}
          if-no-files-found: error
